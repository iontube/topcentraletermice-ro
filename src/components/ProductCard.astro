---
interface Props {
  name: string;
  slug: string;
  affiliate: string;
  specs: Record<string, string>;
  summary?: string;
  badge?: string;
  badgeType?: string;
}
const { name, slug, affiliate, specs, summary, badge, badgeType = 'top' } = Astro.props;
const imgSrc = `/assets/images/products/${slug}.webp`;
const reviewId = 'recenzie-' + name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/-+$/,'');
const specEntries = Object.entries(specs);
---
<article class="card">
  {badge && <span class={`card__badge card__badge--${badgeType}`}>{badge}</span>}
  <div class="card__img">
    <img src={imgSrc} alt={name} width="200" height="145" loading="lazy" />
  </div>
  <div class="card__body">
    <div class="card__specs">
      {specEntries.map(([k, v]) => (
        <span class="card__spec"><strong>{k}</strong><span>{v}</span></span>
      ))}
    </div>
    <h3 class="card__name">{name}</h3>
    {summary && <p class="card__summary">{summary}</p>}
    <div class="card__foot">
      <a href={`/out/${slug}/`} class="card__cta" rel="nofollow sponsored noopener" target="_blank">Vezi Oferta</a>
      <a href={`#${reviewId}`} class="card__review-link">Recenzie</a>
    </div>
  </div>
</article>
