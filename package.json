{
  "name": "topcentraletermice-ro",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-redirects.mjs && astro build",
    "deploy": "npm run build && wrangler pages deploy dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "sharp": "^0.33.0"
  }
}
